name: litgame_server
description: litgame_server
version: 0.0.1
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  meta: ^1.3.0
  shelf: ^1.1.2
  shelf_router: ^1.0.0
  parse_server_sdk: ^3.0.0
  archive: ^3.1.2
  file: ^6.1.0

dev_dependencies:
  pedantic: ^1.11.0
  test: ^1.17.1
